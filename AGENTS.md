# AGENTS.md - Converge API 项目规范

本文件定义了 Converge API 项目的开发规范。所有 AI 编码助手在参与本项目时，必须严格遵守以下规则。

> **通用语言要求**：本项目所有代码注释、日志输出以及 Git 提交信息，**必须使用中文**编写。禁止使用英文写注释、日志和提交信息（专有名词、技术术语、代码标识符引用等除外）。

---

## 1. 代码注释规范

所有代码注释必须完整、清晰、有价值，不得以"显而易见"为由省略。

### 1.1 后端 Java 注释

- **实体类**：每个实体类必须有类注释，说明其用途。每个字段必须有注释，说明字段含义、是否必填、约束等。
- **DTO / VO / 请求参数类**：同实体类规范，类和每个字段都必须有注释。
- **枚举类**：类注释说明用途，每个枚举常量必须有注释。
- **接口方法**：public 方法必须有 Javadoc 注释（`/** ... */`），包括参数说明 (`@param`)、返回值 (`@return`)、异常 (`@throws`)。
- **关键逻辑**：复杂的分支、算法、状态流转、业务规则处理必须有行内注释说明意图。
- **特殊处理**：临时方案、TODO 项、边界条件处理、兼容性 hack 必须加注释并标注原因。

### 1.2 前端 TypeScript/React 注释

- **类型定义**：`interface` / `type` 定义及其每个属性必须有注释。
- **组件 Props**：组件 Props 接口中的每个属性必须有注释。
- **自定义 Hooks**：函数必须有注释说明用途、参数和返回值。
- **复杂逻辑**：状态流转、副作用依赖、数据转换、条件分支必须有注释。
- **API 调用**：请求接口定义必须有注释说明 endpoint、请求参数含义、响应结构。
- **工具函数**：每个导出函数必须有注释说明用途和参数。

---

## 2. 后端日志规范

项目使用 SLF4J + Logback（Spring Boot 默认），日志必须贯穿所有关键流程。

- **接口调用日志**：每个 Controller 接口的入参和调用结果必须记录。使用 `log.info(...)`。
- **任务处理日志**：异步/定时任务必须记录开始、处理进度、完成/失败。
- **关键数据变更**：数据创建、更新、删除操作必须记录操作人和变更内容。
- **异常日志**：捕获异常时必须 `log.error(...)` 记录完整堆栈和上下文信息。
- **日志级别约定**：
  - `ERROR` — 导致流程中断的异常
  - `WARN` — 不应发生但不影响主流程的异常情况（如参数校验失败）
  - `INFO` — 关键业务流程节点（接口调用、任务调度、数据变更）
  - `DEBUG` — 开发调试用细粒度信息

禁止直接使用 `System.out.println` 或 `System.err.println` 代替日志框架。

---

## 3. Git 提交规范

提交必须粒度适中、按功能/修复独立提交，不得攒大量修改一次性提交。

### 3.1 提交消息格式

```
<type>(<scope>): <简短描述>

<详细说明（可选）>
```

### 3.2 类型前缀

| 前缀 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(登录): 添加密码可见性切换` |
| `fix` | Bug 修复 | `fix(认证): 修复令牌刷新竞态条件` |
| `refactor` | 代码重构 | `refactor(API): 将健康检查抽取为独立控制器` |
| `docs` | 文档变更 | `docs: 添加 API 使用示例` |
| `style` | 代码格式 | `style: 使用 prettier 格式化代码` |
| `test` | 测试相关 | `test(服务): 添加登录流程单元测试` |
| `chore` | 构建/配置 | `chore: 升级 spring-boot 至 4.1.0` |

### 3.3 粒度原则

- 每个提交只解决一个问题：一个功能、一个 bug、一次重构。
- 不得将无关更改混入同一提交。
- 提交前使用 `git diff` 自检，确保不包含调试代码、无意义的空行变动等。

---

## 4. 通用原则

- 配置文件（application.yml 等）中的敏感信息使用占位符，禁止硬编码密码/密钥。
- 前端组件保持单一职责，避免巨型组件。
- 后端 Controller 层保持薄层，业务逻辑下沉到 Service 层。
- 禁止在提交中包含自动生成的 IDE 配置、编译产物（target/、dist/、node_modules/ 等）。
- 遵循所在语言的社区编码风格（Java 遵循阿里巴巴 Java 开发手册，TypeScript 遵循 ESLint + Prettier 配置）。
