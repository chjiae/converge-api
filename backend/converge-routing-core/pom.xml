<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.github.chjiae</groupId>
        <artifactId>converge-api</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>converge-routing-core</artifactId>
    <name>Converge Routing Core</name>
    <description>AI 网关静态路由纯 Java 核心</description>

    <dependencies>
        <!-- 共享快照契约，routing-core 不依赖控制面或网关运行时。 -->
        <dependency>
            <groupId>com.github.chjiae</groupId>
            <artifactId>converge-contract</artifactId>
        </dependency>

        <!-- routing-core 依赖边界与路由语义测试 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
