/**
 * 通知列表页面
 *
 * 占位组件，待后续实现。
 * 所有已登录用户均可访问。
 */
export default function NotificationListPage() {
  return (
    <div className="flex items-center justify-center py-20">
      <p className="text-muted-foreground">通知列表 — 开发中</p>
    </div>
  )
}
